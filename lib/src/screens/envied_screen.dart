import 'package:flutter/material.dart';

class EnviedScreen extends StatelessWidget {
  const EnviedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Flutter Dotenv'),
      ),
    );
  }
}
